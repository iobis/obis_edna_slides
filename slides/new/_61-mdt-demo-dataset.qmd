## Laura's Elegant Solution: MDT to IPT

::: {.mdt-routes}
```{=html}
<svg viewBox="0 0 1000 300" role="img" aria-labelledby="mdt-routes-title" style="width:100%; height:auto;">
  <title id="mdt-routes-title">Two routes into the IPT: through the MDT, or straight there with tabular files</title>
  <defs>
    <marker id="mdtArrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#6C757D"/>
    </marker>
  </defs>
  <!-- route one: through the MDT -->
  <text x="10" y="88" font-size="15" font-weight="600" fill="#0E9E82" letter-spacing="0.06em">WITH MDT</text>
  <rect x="170" y="50" width="220" height="64" rx="8" fill="#F8F9FA" stroke="#DEE2E6"/>
  <text x="280" y="88" text-anchor="middle" font-size="18" fill="#156082">eDNA standard outputs</text>
  <line x1="392" y1="82" x2="432" y2="82" stroke="#6C757D" stroke-width="2" marker-end="url(#mdtArrow)"/>
  <rect x="440" y="50" width="140" height="64" rx="8" fill="#E9FBF7" stroke="#0E9E82" stroke-width="2"/>
  <text x="510" y="88" text-anchor="middle" font-size="20" font-weight="600" fill="#0E9E82">MDT</text>
  <line x1="582" y1="82" x2="622" y2="82" stroke="#6C757D" stroke-width="2" marker-end="url(#mdtArrow)"/>
  <rect x="630" y="50" width="140" height="64" rx="8" fill="#F8F9FA" stroke="#DEE2E6"/>
  <text x="700" y="88" text-anchor="middle" font-size="20" font-weight="600" fill="#156082">IPT</text>
  <text x="510" y="140" text-anchor="middle" font-size="15" fill="#0E9E82">MDT does the Darwin Core mapping</text>
  <!-- route two: straight to the IPT -->
  <text x="10" y="228" font-size="15" font-weight="600" fill="#6C757D" letter-spacing="0.06em">STRAIGHT TO IPT</text>
  <rect x="170" y="190" width="220" height="64" rx="8" fill="#F8F9FA" stroke="#DEE2E6"/>
  <text x="280" y="228" text-anchor="middle" font-size="18" fill="#156082">Tabular files</text>
  <line x1="392" y1="222" x2="622" y2="222" stroke="#6C757D" stroke-width="2" stroke-dasharray="6 5" marker-end="url(#mdtArrow)"/>
  <rect x="630" y="190" width="140" height="64" rx="8" fill="#F8F9FA" stroke="#DEE2E6"/>
  <text x="700" y="228" text-anchor="middle" font-size="20" font-weight="600" fill="#156082">IPT</text>
  <text x="510" y="280" text-anchor="middle" font-size="15" fill="#6C757D">you do the Darwin Core mapping yourself</text>
</svg>
```
:::

<style>
.reveal .mdt-routes {
  margin-top: 10px;
}
/* The theme caps slide images, but this is inline SVG rather than an img, so it
   is sized here instead. */
.reveal .mdt-routes svg {
  max-height: 420px;
  display: block;
  margin: 0 auto;
}
</style>

::: {.notes}
Two ways into the same place. Both end at the IPT, and both produce a Darwin Core Archive; the difference is who does the mapping.

Laura's route runs the standard outputs of an eDNA pipeline through the MDT, which reads the OTU table, the taxonomy and the sample sheet and writes Darwin Core for her. The other route is the one most people picture: build the tables yourself, in the right terms, and hand them to the IPT directly. That is drawn dashed because nothing helps you along the way.

Neither is wrong. The point of the next few slides is what the MDT expects in return for doing that work.
:::

## Laura's Data

:::: {.columns .mdt}

::: {.column width="40%"}
**Two workbooks — one per primer**

| Workbook | OTUs | Samples |
|---|---|---|
| `vert_…` **Vert01** | 460 | 15 |
| `teleo_…` **Tele01** | 113 | 13 |

- One station sampled over 5 dates
- 2-3 locations sampled within the station on each date
- Each sample == 120L
:::

::: {.column width="60%"}
**Each sheet is one MDT table**

| In the file | MDT table | Becomes |
|---|---|---|
| `otu_table` | **OTU_table** | reads → `organismQuantity` |
| `taxon` | **Taxonomy** | `scientificName`, `DNA_sequence` |
| `sample` | **Samples** | `sampleID` → **`eventID`** |
| `defaultValues` | **Study** | terms applied to *every* record |
:::

::::

::: {style="font-size:0.6em; background:#EAF2F6; border-left:4px solid #156082; padding:8px 14px; border-radius:4px;"}
MDT guide: *"Mixed datasets (e.g., different primer sets used for the same set of samples) should be published separately."* — which is why this is **two** workbooks, not one.
:::

::: {.notes}
This is the same idea as the Study table doing what defaultValues does: say a thing once instead of on every row. Nine terms here — env_medium, target_gene, both primers and their names, sop, seq_meth, otu_db — cover all 460 x 15 records.

The two-workbook split is not an accident or an oversight. The guide is explicit: different primer sets on the same samples are separate datasets, because an OTU from Vert01 and an OTU from Tele01 are not comparable units. Note the OTU numbering runs continuously across both files, so they clearly came out of one pipeline and were split afterwards.

Sample IDs become eventID. That is the hinge between this spreadsheet and everything we said about Event core.
:::
